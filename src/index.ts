// our entry point into this library

export { HammerEngine } from './engines/hammer_engine';

// default export will be the hammer engine
import HammerEngine from './engines/hammer_engine';
export default HammerEngine;
